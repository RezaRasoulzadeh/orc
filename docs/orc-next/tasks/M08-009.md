# M08-009 — Add explicit typed curation for Controller memory maintenance judgment

**Status:** Done
**Milestone:** M08 — Experience dataset

## Objective

Add one capability-local curation adapter for an already-produced Controller memory-maintenance judgment, projecting the existing typed `ControllerMemoryMaintenanceInput` and canonical `ControllerMemoryMaintenanceResult` into one M08-001 experience example under explicit trusted verification metadata.

Curate the maintenance judgment itself only. Later mutation proposal validation, maintenance grants, mutation execution, selected-target composition, and durable memory state are downstream boundaries and remain outside the reasoning target.

## Canonical capability

`controller.memory_maintenance`

## Result

Implemented at `6af29dc8fdd3a444ce5903ce308289cdd882b370`.

The implementation adds the capability-local `ControllerExperienceMemoryMaintenanceRequest`, `ControllerExperienceMemoryMaintenanceError`, and `OrcApp::create_controller_memory_maintenance_experience_example(...)` path. It preserves the exact validated `ControllerMemoryMaintenanceInput` and exact validated `ControllerMemoryMaintenanceResult`, fixes capability identity in code, and persists only through the existing M08-001 experience API.

Production validation remains authoritative:

- `ControllerMemoryMaintenanceInput::validate()` validates the exact supplied request/target/context;
- `ControllerMemoryMaintenanceResult::validate(&input)` validates observed and accepted outputs;
- `Create` remains invalid;
- `Correct`, `Supersede`, and `Remove` remain bound to the exact supplied target;
- Correct/Supersede replacements must preserve target kind, scope, and subject.

Correction semantics compare exact canonical serialized observed and accepted outputs. Equal outputs reject correction metadata/`Corrected`; differing outputs require `Corrected`, explicit correction metadata, and exact preservation of the observed result in `correction.original_output`.

No storage lookup, target selection, inference, harvesting, hooks, grant handling, mutation execution, runtime `MemoryKind::Experience` change, generic curation framework, export, splitting/balancing, embeddings, fine-tuning, provider fallback, provider token hard cap, or Python dependency was introduced.

## Completion evidence

Independent review: **PASS**.

Validation:

- focused M08-009: 12 passed;
- M08-001 through M08-008 regressions passed;
- relevant M06 maintenance and M07 grant/composed-step/selection regressions passed;
- `cargo test --lib`: 410 passed;
- `cargo test --features llama-cpp --lib`: 416 passed;
- normal and llama-cpp `cargo clippy --all-targets -- -D warnings` passed;
- `cargo fmt --check` passed;
- `git diff --check` passed.

No real-Qwen evaluation was required because maintenance inference prompt/schema/parser/bounds were unchanged. A production result-size failure is not representable after valid typed maintenance validation because valid replacement drafts are bounded below the 32 KiB result limit; M08 payload and complete-record bounds are covered separately.

The next repository-grounded M08 reasoning seam is Controller memory-maintenance target selection.