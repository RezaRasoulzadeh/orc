# M05-009 — Replace Lead intake judgment with supervised Controller routing

**Status:** Done
**Milestone:** M05 — Planning and Lead unification

## Objective

Complete M05 by moving the remaining normal-workflow Lead intake judgment into the local Controller while keeping intake persistence, workflow transitions, user gates, DIRECT_TASKS application, Plan routing, and all mutation authority deterministic and kernel-owned.

After this task, the normal supervised Controller workflow no longer requires a legacy Lead or Planner semantic backend for intake, Plan generation, Plan review, or Plan revision. Legacy Lead/Planner APIs and durable historical records remain compatibility surfaces.

## Result

Implemented and independently reviewed PASS.

- Controller intake supports exactly `DirectTasks`, `PlanRequired`, and `UserDecisionRequired` through bounded strict structured inference.
- Normal supervised workflow intake no longer invokes the legacy Lead semantic backend.
- `DirectTasks` reuses canonical deterministic application.
- `PlanRequired` enters the M05-008 Controller Plan generation/review/revision path.
- User-decision state is durably workflow-bound and supports restart-safe, runtime-free recovery of already-persisted boundaries.
- Recovery prevents cross-workflow adoption and duplicate inference/mutation.
- Controller output remains judgment/content only; workflow identity, lifecycle state, persistence identity, authorization, and mutation remain kernel-owned.
- No fabricated Lead/Planner/provider lineage was introduced.
- Legacy Lead/Planner compatibility APIs and historical decoding remain functional.
- Real Qwen3 intake evaluation passed strict 3/3 and semantic 3/3.
- Normal and llama.cpp-feature test suites, Clippy, formatting, and diff checks passed.

Implementation commit: `5e7cf0d6206052b043239b7676c272276361b08c`.

## Acceptance

All acceptance criteria passed. No normal supervised Controller workflow semantic dependency on legacy Lead/Planner remains. M05 is complete; remaining Lead/Planner machinery is compatibility-only and is not a reason to add another M05 cleanup task.