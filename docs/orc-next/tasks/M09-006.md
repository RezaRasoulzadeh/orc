# M09-006 — Provision and measure the canonical specialization source

**Status:** Done
**Milestone:** M09 — Controller specialization

## Objective

Establish the actual canonical Controller specialization source before trainer-specific transformation or training. Verify the configured global registry through the normal operator path and capture deterministic M08-011 inventory and M09-001 Active snapshot evidence without mutating canonical examples or inventing readiness.

## Completed scope

The supported `orc init` path provisioned the missing experience schema in the authoritative registry `/home/reza/.local/share/orc/agents.db`. Existing canonical application APIs then measured inventory schema 1 with total 0, Active 0, Retired 0; snapshot schema 1 with count 0 and empty IDs/order/examples; and zero observed records across all nine capabilities. Lifecycle, outcome, and verification-basis category counts are zero. The prior missing-table state is now superseded by a measured empty dataset.

The measurement used a temporary non-production read harness, removed afterward. No dedicated CLI command or production code was required. No canonical records were created, changed, retired, or deleted. No export, transformation, training, model acquisition, or runtime/default change occurred.

## Review and completion evidence

The initial independent review returned REVISE for missing exact repository revision and imprecise local-versus-remote task-file wording. Both were corrected without changing measurement facts. Independent re-review returned PASS against the complete task contract.

The report records inspected local HEAD `52a37429c8438e626106fd25fa23363df7496109`, the remote task-file advancement, registry authority/provisioning evidence, exact inventory/snapshot results, capability coverage, factual distributions, and remaining blockers. Documentation-only `git diff --check` passed, including the untracked report. No code validation was required.

**Measurement report:** `docs/orc-next/M09-006-canonical-specialization-source-measurement.md`

**Implementation commit:** `8481d1874796a520df3e29fd4993c122b94b0ae0`

The exact implementation commit was independently verified as remote `main` HEAD before canonical closure. The report was committed through an isolated clean worktree based on the latest remote main; unrelated local modifications were preserved.

## Remaining boundary

The measured canonical source is empty, not training-ready. No dataset size, quality threshold, balance target, split, weighting, or trainer suitability is inferred. Backend selection remains deferred; ms-swift remains conditional. M09-001–004 and D-006/D-007 retain their authority. Actual training remains blocked on source data, approved model artifacts, usable hardware, trainer qualification, external transform review, conversion validation, and a real baseline. Later work must establish these prerequisites independently before execution or promotion.
