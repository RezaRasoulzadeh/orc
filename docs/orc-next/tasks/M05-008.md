# M05-008 — Route supervised Plan workflow through Controller capabilities

**Status:** Done
**Milestone:** M05 — Planning and Lead unification

## Objective

Integrate the completed Controller Plan generation, review, revision, and authorized persistence boundaries into Orc's persisted Plan workflow so normal supervised Plan progression no longer depends on legacy Planner/Lead semantic calls, while preserving deterministic workflow transitions, approval/application gates, restart safety, and legacy compatibility.

## Result

Implemented in `6d2224d9696c18790b259330e5f28651d7b13a52`.

- Added persisted `WorkflowPlanPath` with legacy-compatible defaults.
- Added truthful provider-less Controller Plan/review/revision workflow outcomes and adapters.
- Routed Controller Plan generation, review, and revision through M05-001 through M05-007 semantic and authorized persistence boundaries.
- Added exact workflow-bound durable Plan/review/revision lineage for restart recovery; removed heuristic cross-workflow adoption risk.
- Normal continuation can recover already-persisted Controller boundaries without a runtime; runtime is required only for fresh inference.
- Corrected Controller `UserDecisionRequired` resume semantics for persisted `UnderReview` Plans.
- Preserved deterministic workflow transitions, revision limits, configured user approval, canonical `ApplyPlan`, task lifecycle, and legacy compatibility paths.
- Luna+High source review initially found three behavioral blockers; all were corrected and re-review passed.

Validation: 21 workflow tests passed; `cargo test --lib` 364 passed; `cargo test --features llama-cpp --lib` 370 passed; full integration suite, normal and feature Clippy, `cargo fmt --check`, and `git diff --check` passed. Inference semantics did not change, so Qwen was not rerun.

## Preserve

- M05-001 through M05-007 Controller Plan semantics and authorization boundaries.
- Persisted `WorkflowEngine` transition legality and restart safety.
- Existing Plan status/version/provenance/review semantics.
- Existing `ApplyPlan` task creation/application semantics.
- Existing user approval/cancellation gates.
- Existing task execution lifecycle after Plan application.
- Legacy Lead/Planner compatibility APIs until final M05 unification/cleanup.
