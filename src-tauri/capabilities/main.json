{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permissions for the privileged Orc application window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
