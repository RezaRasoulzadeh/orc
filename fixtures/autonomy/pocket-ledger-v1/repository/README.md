# Pocket Ledger

A dependency-free Rust repository used by Orc's external autonomy benchmark.

Input records use `id|label|category|active` or `inactive`. The fixture starts
without a suspended state; introducing that state is the fifth benchmark task.
