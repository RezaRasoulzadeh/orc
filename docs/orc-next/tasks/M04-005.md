# M04-005 — Route semantic revision non-convergence into supervised Controller recovery

**Status:** Done
**Milestone:** M04 — Recovery intelligence

## Objective

Migrate semantic revision non-convergence from automatic economy escalation into the supervised M04 Controller recovery boundary while preserving deterministic detection and canonical review/revision/run/economy facts.

## Result

Implemented narrowly. Semantic non-convergence's automatic economy escalation and exhaustion condition were removed while deterministic repeated-blocker detection remains through `record_semantic_revision_non_convergence`.

The path now persists bounded `semantic_revision_non_convergence_detected` lifecycle evidence. Review evidence, revision contract/currentness, lineage, execution history, and attempt evidence remain preserved. Revision economy resolution receives no automatic escalation request; canonical eligibility, quota, model/tier, invocation, and usage facts remain unchanged.

M04-001 recovery inspection exposes `ResumeRevision` only when canonical revision legality permits it and rejects generic `Requeue` where it would discard actionable revision lineage. Existing exact non-convergence acknowledgement semantics remain unchanged. M04-002 recommendation and M04-003 supervised authorization/execution remain the recovery decision/execution boundary.

No replacement retry/escalation/resume heuristic or autonomous continuation loop was introduced.

## Validation

- Dispatch/revision tests: 87 passed.
- Economy/lifecycle tests: 12 passed.
- Lifecycle tests: 22 passed.
- Operations tests: 8 passed.
- Queue tests: 25 passed.
- `cargo test --lib`: 336 passed.
- `cargo test --features llama-cpp --lib`: 342 passed.
- Normal and feature clippy: passed.
- `cargo fmt --check`: passed.
- `git diff --check`: passed.
- Qwen not rerun because production recovery prompt/schema/parser semantics were unchanged.
- Luna + High source review: `PASS`.

## Next boundary

General scheduler/economy escalation infrastructure and unrelated retry/selection policies remain for later supervised migration.